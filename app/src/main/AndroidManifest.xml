<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.harsh.mock_fb">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_practice"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_practice_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activity.LoginActivity"
            android:screenOrientation="portrait" />
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity
            android:name=".activity.ProfilePage"
            android:screenOrientation="portrait">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".activity.GoogleLoginNext" />
        </activity>
        <activity
            android:name=".activity.IGLogin"
            android:screenOrientation="portrait" />
        <activity android:name=".activity.WelcomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.GoogleLogin" />
        <activity android:name=".activity.GoogleLoginNext" />
        <activity android:name=".activity.InfoActivity" />
    </application>

</manifest>